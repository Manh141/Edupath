import BecomeInstructorPage from "@/components/pages/BecomeInstructorPage";

export const metadata = { title: "Become an Instructor — EduPath" };

export default function Page() {
  return <BecomeInstructorPage />;
}
