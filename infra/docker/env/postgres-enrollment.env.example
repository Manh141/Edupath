# Local docker-compose Postgres for enrollment-service.
# For production, use a managed Postgres and a strong unique password.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=edupath_enrollment
